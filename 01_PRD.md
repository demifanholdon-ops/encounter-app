# Encounter PRD · 产品需求文档

> 两天黑客松 Demo · v1.0 · 2026-05-28

---

## 一、背景

线下科技活动(黑客松/AI Coffee Chat/分享会)现场几百人,
用户无法逐个聊。手动筛选 18 人要 3 小时,500 人要 83 小时。

Encounter 把这个流程压缩到 30 秒:
碰一下对方 NFC → 30 秒得到一张"作战卡片"
(总分 + 三维度 + 三段式建议)。

---

## 二、目标(两天必交付)

**主目标**:31 号路演时,评委亲手碰一张 NFC,
30 秒内看到一张完整的 AI 作战卡片,并能切到列表模式看 S/A/B 排序。

**不做**:用户系统、登录、支付、双 AI 互聊、安卓连扫、真上链 NFT、
端到端加密、多语言、移动 App 原生开发。

---

## 三、核心用户故事

```
作为参加线下科技活动的用户,
我希望碰一下对方的 NFC 贴纸,
让 AI 替我读懂对方、并告诉我 "对我有什么价值 + 该问什么",
这样我可以在 8 小时现场快速识别该深聊的 5 个人。
```

---

## 四、功能列表(P0 = 必做 / P1 = 时间够再做)

### P0 — 主链路(demo 必须能跑)

| ID | 功能 | 验收标准 |
|---|---|---|
| F1 | 首次使用,采集用户三层诉求 | 用户进入首页,文字输入长/中/短期目标,存 localStorage |
| F2 | NFC 碰一下,识别对方身份 | 安卓/iPhone 都能读 NFC URL,跳转到 `/encounter/[id]` |
| F3 | AI 输出作战卡片 | 30 秒内返回 JSON(见 Prompt 规格),失败有兜底 |
| F4 | 渲染作战卡片 | 显示总分 + 三维度 + 三段式 + 标签,样式参考 PPT 第 6 张 |
| F5 | 列表模式(群体感) | 右上角切换,8 个预置假人 + 已扫的人按总分排序,S/A/B 分组 |
| F6 | 数据存储 | 测试人用 JSON 文件,扫到的人存 localStorage |

### P1 — 加分项(有时间再做)

| ID | 功能 | 说明 |
|---|---|---|
| F7 | 卡片点头像翻转看 profile | 次要信息藏后面 |
| F8 | 扫前对话框:改本场短期目标 | 体现"活"的动态性 |
| F9 | 列表模式切换排序视角 | 按合作分/认知分/战略储备过滤 |
| F10 | 容错兜底 | API 挂时显示预设黄金案例 |

### 明确不做(放 PPT 路线图)

- 双 AI 分身互聊
- 真上链 NFT / 钱包集成
- 安卓 Web NFC 连扫一墙
- 多用户/账号体系

---

## 五、关键页面

```
/                          → 首页(采集三层诉求,首次使用)
/scan                      → 群体列表页(默认展示已扫的人 + 测试人)
/encounter/[id]            → 单人作战卡片(NFC 跳转目标)
/api/score                 → POST,接收 user + target,返回 EncounterCard
```

---

## 六、数据结构

### 用户(self,存 localStorage)
```ts
type UserContext = {
  longTerm: string       // "AI 硬件出海创业"
  midTerm: string        // "扩展认知和人脉"
  shortTerm: string      // "本场找 Web3 / AI 硬件方向的人"
  background: string     // 自己的能力背景
  activityContext: string // "武汉 Rebase Web3 黑客松 2026-05-27 至 31"
}
```

### 测试人(存 `data/people.json`)
```ts
type PersonData = {
  id: string             // NFC URL 里的 uid
  name: string
  role: string           // "AI 硬件创业者"
  bio: string
  recentActivity: string // 最近动态
  publicWorks?: string   // 公开作品/内容
  avatar?: string        // 头像 URL 或 emoji
}
```

### AI 输出
见《AI 打分 Prompt 规格》第四节 EncounterCardSchema,
**禁止修改字段**。

---

## 七、验收清单(31 号路演前必过)

- [ ] 评委用安卓手机碰 NFC → 自动跳转 → 30 秒内出卡片
- [ ] 评委用 iPhone 碰 NFC → 自动跳转 → 30 秒内出卡片
- [ ] 卡片显示:总分 + S/A/B + 三维度(含战略储备标记) + 三段式 + 3 个开口问题
- [ ] 切到列表模式,8 个预置人按总分排序,刚扫的新人实时插入
- [ ] 网络/API 出问题时,有兜底文案(不白屏)
- [ ] 3 个"黄金案例"必跑通(S 级/A 级/B 级各一个)

---

## 八、不在范围内但要提的事

- **资本/评委追问"为什么 NFC 不用扫码"**:产品答辩 PPT 有完整答案
  (NFC 是硬件载体延展性,扫码只能是图)
- **追问"群体扫描怎么做"**:答"今天 demo 演单张 + 列表呈现群体,
  规模化连扫是产品路线图(见 PPT)"
- **追问"用户没有 Lens 怎么办"**:答"我们支持 Twitter/GitHub/简介任何身份卡,
  Lens 是 Web3 用户的进阶选项,不是门槛"
